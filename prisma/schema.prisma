generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:test.db"
}

model test {
  id       Int    @id @default(autoincrement())
  name     String
  lastName String
}
